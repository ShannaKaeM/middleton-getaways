{# Basic Button Component for Hero-1 #}
<a href="{{ url|default('#') }}" 
   class="umbral-button umbral-button--{{ variant|default('primary') }} umbral-button--{{ size|default('medium') }}"
   {% if target %}target="{{ target }}"{% endif %}>
   {{ text|default('Button') }}
</a>