{# Raycast Hero Component Template #}
{# Styles and scripts are auto-enqueued by compileComponent() #}

{# Raycast Hero Component HTML #}
<section id="{{ component_id }}" class="umbral-raycast-hero" data-cards-layout="{{ cards_layout }}" data-cards-animation="{{ cards_animation }}">
    
    {# Background with floating elements #}
    <div class="hero-background">
        <div class="floating-elements">
            <div class="floating-element floating-1"></div>
            <div class="floating-element floating-2"></div>
            <div class="floating-element floating-3"></div>
            <div class="floating-element floating-4"></div>
        </div>
        <div class="gradient-mesh"></div>
    </div>
    
    <div class="hero-container">
        
        {# Feature cards - left side (only for 'sides' layout) #}
        {% if cards_layout == 'sides' %}
        <div class="feature-cards-left">
            {% set left_cards = feature_cards|slice(0, 2) %}
            {% for card in left_cards %}
            <div class="feature-card card-animate-left">
                <div class="card-icon">{{ card.icon }}</div>
                <div class="card-content">
                    <h4 class="card-title">{{ card.title }}</h4>
                    <p class="card-description">{{ card.description }}</p>
                </div>
            </div>
            {% endfor %}
        </div>
        {% endif %}
        
        {# Main hero content #}
        <div class="hero-main" data-alignment="{{ title_alignment }}">
            
            {# Hero text #}
            <div class="hero-text">
                <h1 class="hero-title">{{ hero_title }}</h1>
                {% if hero_subtitle %}
                <p class="hero-subtitle">{{ hero_subtitle }}</p>
                {% endif %}
            </div>
            
            {# Command palette interface #}
            <div class="command-palette">
                <div class="search-container">
                    <div class="search-icon">
                        <svg width="20" height="20" viewBox="0 0 20 20" fill="none">
                            <path d="M9 17A8 8 0 1 0 9 1a8 8 0 0 0 0 16zM20 20l-4.35-4.35" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                        </svg>
                    </div>
                    <input type="text" class="search-input" placeholder="{{ search_placeholder }}" readonly>
                    <div class="search-shortcut">{{ search_shortcut_key }}</div>
                </div>
                
                {# Search shortcuts #}
                {% if show_shortcuts_section and shortcuts %}
                <div class="shortcuts-container">
                    {% for shortcut in shortcuts %}
                    <div class="shortcut-item">
                        <span class="shortcut-icon">{{ shortcut.icon }}</span>
                        <span class="shortcut-text">{{ shortcut.text }}</span>
                        <span class="shortcut-key">{{ shortcut.keystroke }}</span>
                    </div>
                    {% endfor %}
                </div>
                {% endif %}
            </div>
            
            {# CTA Button #}
            {% if cta_text %}
            <div class="hero-cta">
                <a href="{{ cta_url }}" class="cta-button cta-style-{{ cta_style }}">
                    {{ cta_text }}
                    {% if show_cta_arrow %}
                    <svg width="16" height="16" viewBox="0 0 16 16" fill="none" class="cta-arrow">
                        <path d="M3.33334 8H12.6667M12.6667 8L8.00001 3.33333M12.6667 8L8.00001 12.6667" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    </svg>
                    {% endif %}
                </a>
            </div>
            {% endif %}
            
        </div>
        
        {# Feature cards - right side (only for 'sides' layout) #}
        {% if cards_layout == 'sides' %}
        <div class="feature-cards-right">
            {% set right_cards = feature_cards|slice(2, 2) %}
            {% for card in right_cards %}
            <div class="feature-card card-animate-right">
                <div class="card-icon">{{ card.icon }}</div>
                <div class="card-content">
                    <h4 class="card-title">{{ card.title }}</h4>
                    <p class="card-description">{{ card.description }}</p>
                </div>
            </div>
            {% endfor %}
        </div>
        {% endif %}
        
    </div>
    
    {# Feature cards - grid below (for 'grid_below' layout) #}
    {% if cards_layout == 'grid_below' and feature_cards %}
    <div class="feature-cards-grid">
        {% for card in feature_cards %}
        <div class="feature-card card-animate-up">
            <div class="card-icon">{{ card.icon }}</div>
            <div class="card-content">
                <h4 class="card-title">{{ card.title }}</h4>
                <p class="card-description">{{ card.description }}</p>
            </div>
        </div>
        {% endfor %}
    </div>
    {% endif %}
    
    {# Feature cards - scattered (for 'scattered' layout) #}
    {% if cards_layout == 'scattered' and feature_cards %}
    <div class="feature-cards-scattered">
        {% for card in feature_cards %}
        <div class="feature-card card-scattered card-{{ loop.index }}">
            <div class="card-icon">{{ card.icon }}</div>
            <div class="card-content">
                <h4 class="card-title">{{ card.title }}</h4>
                <p class="card-description">{{ card.description }}</p>
            </div>
        </div>
        {% endfor %}
    </div>
    {% endif %}
    
</section>