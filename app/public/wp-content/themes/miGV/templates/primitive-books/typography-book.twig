{#
  Typography Primitive Book - Self-Sufficient Design Tokens
  
  This primitive loads actual typography values from JSON file.
  It is the single source of truth for typography in the design system.
  
  Usage:
  {% include 'primitive-books/typography-book.twig' with {
    font_family: 'inter',
    font_size: 'large',
    font_weight: 'semiBold',
    line_height: 'tight',
    letter_spacing: 'normal'
  } %}
#}

{# Load typography tokens from JSON #}
{% set typography_tokens = load_primitive('typography') %}

{# Export tokens to parent scope if requested #}
{% if export_tokens is defined and export_tokens %}
  {% set _parent = _context %}
  {% do _parent.typography_tokens = typography_tokens %}
{% endif %}

{# Apply typography styles based on parameters #}
{% if font_family %}
  font-family: {{ typography_tokens.font_families[font_family] ?? font_family }};
{% endif %}

{% if font_size %}
  font-size: {{ typography_tokens.font_sizes[font_size] ?? font_size }};
{% endif %}

{% if font_weight %}
  font-weight: {{ typography_tokens.font_weights[font_weight] ?? font_weight }};
{% endif %}

{% if line_height %}
  line-height: {{ typography_tokens.line_heights[line_height] ?? line_height }};
{% endif %}

{% if letter_spacing %}
  letter-spacing: {{ typography_tokens.letter_spacings[letter_spacing] ?? letter_spacing }};
{% endif %}
